#include "main.h"

/**
* int _islower - Finds if a letter is lowercase.
* @c: The letter to be tested.
* Return: An integer, 1, if lowercase, 0, if not.
*/
int _islower(int c);
{
	if (c >= 97 && c <= 122)
		return (1);
	else
		return (0);
}
